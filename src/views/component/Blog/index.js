import { useParams } from "react-router-dom";

function Blog(props) {

  // const params = useParams();
  // console.log(params);

    return (
      <div>
        블로그 페이지
      </div>
    )
}

export default Blog;